#include<stdio.h>
int main(){
    int length,breath;
    printf("what is length?");
    scanf("%d",&length);
    printf("what is breath?");
    scanf("%d",&breath);
    printf("the area of rectangk=le is %d",length*breath);
    return 0;
}